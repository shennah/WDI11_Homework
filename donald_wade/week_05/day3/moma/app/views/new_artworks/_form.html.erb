<%= form_for(@new_artwork) do |f| %>
<% if @new_artwork.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@new_artwork.errors.count, "error") %>
    prohibited this new_artwork from being saved:</h2>

  <ul>
    <% @new_artwork.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.label :year %><br>
  <%= f.number_field :year %>
</div>
<div class="field">
  <%= f.label :medium %><br>
  <%= f.text_field :medium %>
</div>
<div class="field">
  <%= f.label :artist_id %><br>
  <%= f.text_field :artist_id %>
  <%= select :artist, :name, Artist.all.collect {|a| [ a.name, a.id ] }%>

  <% if false %>

  <%end%>
  <% if false %>
  <%= select_tag "artist", "<option>Salvador Dali</option>" %>
  <% end %>

  <% if false %>
  <%= f.select("artist", "name", Artist.all.collect {|a| [ a.name, a.id ] }, {include_blank: 'None'})%>

  <% end %>

</div>
<td></td>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>